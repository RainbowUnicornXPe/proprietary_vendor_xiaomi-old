# Property to enable security check
persist.sys.strict_op_enable=true

persist.radio.phone.matchnum=11
# Property to force the message to be sent on CDMA dedicated channel
persist.radio.force_on_dc=true

#Property to enable emergency button on non-secure lock screen
persist.radio.emgcy_btn_onswipe=true

# Set Network mode option #
ro.telephony.default_network=22,20
